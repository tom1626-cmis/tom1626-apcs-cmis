public class DLP extends Television
{
    public DLP( String initialModel, double initialPrice ) 
    { 
        super("DLP " + initialModel, initialPrice);
    } 
    public String toString()
    {
        String output = super.toString();
        return output;
    }
}